import React from 'react'

import SearchCompContainer from './SearchCompContainer'

const SearchComp = ({values}) => {
  
  
  return (
    <React.Fragment >
      <SearchCompContainer values={values} />      
    </React.Fragment>
  )
}

export default SearchComp